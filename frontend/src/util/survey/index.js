import SurveyDialogContainer from './SurveyDialogContainer';

/* 
recoil을 사용하여, 상태를 관리한다.
setOpen을 통해, open을 하면, survey dialog를 띄울 수 있다.

  // survey dialog state
  const [open, setOpen] = useRecoilState(surveyDialogState);

  // survey dialog toggle
  const handleClickOpen = () => {
    setOpen(true);
  };

*/

export default SurveyDialogContainer;
